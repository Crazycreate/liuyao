# 复制为 .env 并按需填写。.env 已被 .gitignore 忽略,切勿提交。
#
# 起卦 + 装卦是本地确定性计算,不需要任何 key、不花钱。
# 只有「AI 断卦」用到下面的 provider。默认 pollinations:免费、免 key、开箱即用。
#
# ⚠️ 注意:默认的 pollinations 是免费小模型(推理型),对六爻这种长断常常吐空,
#    只适合"先跑通"。真要断卦,强烈建议改用 ollama(本地免key)或 glm(免费需key)。
#
# 可选:pollinations(默认,免key)/ ollama(本地免key,推荐)/ glm(免费需key,推荐)/ gemini / groq / anthropic / custom
AI_PROVIDER=pollinations

# 想要更好的中文断卦质量,推荐免费的智谱 GLM(需 key,国内直连):
# AI_PROVIDER=glm
# GLM_API_KEY=

# 本地 Ollama(免 key、不联网):
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434/v1
# AI_MODEL_REPORT=qwen2.5:32b
# AI_MODEL_CHAT=qwen2.5:32b

# 付费、质量最高:
# AI_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
